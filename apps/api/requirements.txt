# API dependencies (managed by Poetry)
# Run `poetry install` from the project root to install all dependencies.
# This file is for reference only — Poetry is the canonical package manager.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
sqlalchemy>=2.0.36
psycopg[binary]>=3.2.0
